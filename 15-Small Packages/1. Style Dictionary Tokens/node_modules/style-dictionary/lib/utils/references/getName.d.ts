/**
 * Returns the paths name be joining its parts with separator '.'.
 *
 * @private
 * @param {string[]} path
 * @returns {string} - The paths name
 */
export default function getName(path: string[]): string;
