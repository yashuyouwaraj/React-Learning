import { TransformedToken } from 'style-dictionary/types';
/**
 * @name isStrokeStyle
 * @type filter
 * @description only returns tokens of type `strokeStyle`
 */
export declare const isStrokeStyle: (token: TransformedToken) => boolean;
