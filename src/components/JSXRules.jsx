import React from 'react'

const JSXRules = () => {
  return (
    <div>
        <h1>JSX Rules</h1>
        <p>JSX must have one parent element.</p>
        <p>JSX elements must be closed.</p>
        <p>JSX attributes must be written in camelCase.(eg. ClassName instead of class)</p>
    </div>
  )
}

export default JSXRules